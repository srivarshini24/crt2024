#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int sum,n;
    sum=0;
    do{
        scanf("%d",&n);
        if(n!=-1)
            sum =sum+n;
    }
    while(n!=-1);
    printf("%d",sum);  
    return 0;
}
